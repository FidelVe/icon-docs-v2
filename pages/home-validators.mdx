---
description: 'L1 Chain Built for Interoperability'
---

import React from 'react';
import Image from 'next/image';
import { useTheme } from 'next-themes';
import { useEffect, useState } from 'react';

export const Validatorspage = () => {

  return (

    <main className="font-montserrat max-w-7xl min-h-none sm:min-h-screen mx-auto space-y-0 mt-20 sm:mt-0 sm:space-y-10 dark:text-zinc-300 text-zinc-700 border dark:border-black/0 border-white/0 ">
      
        <section className="py-4 md:pt-20 relative px-4 sm:px-16">
            <h1 className="text-center sm:text-left tracking-widest text-[9vw] sm:text-[50px] lg:text-[70px] font-bold font-lemon leading-none relative z-1 dark:text-white text-black">Secure ICON and earn</h1>
            <h2 className="hidden sm:block w-7/12 text-[14px] md:text-[16px] lg:text-[22px] opacity-80 my-4">Validators on ICON are rewarded according to the IISS 4.0 monetary policy. Help us secure and decentralize the ICON blockchain today!</h2>            
        </section>

        <section className="flex space-x-4 my-4 leading-normal px-4 sm:px-16">
                <a href='/validator-rewards' className=' border-xteal w-fit !no-underline px-4 py-8 dark:border-white border block'><p>Validator rewards</p></a>
                <a href='/how-to-register-a-validator-node' className=' border-xteal w-fit !no-underline px-4 py-8 dark:border-white border block'><p>How to register a validator node</p></a>
        </section>

    </main>

  );
};

export default Validatorspage;